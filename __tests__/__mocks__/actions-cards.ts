import { vi } from "vitest"

export const updateDailyCard = vi.fn().mockResolvedValue(undefined)
export const createDailyCard = vi.fn().mockResolvedValue(undefined)
export const getDailyCard = vi.fn().mockResolvedValue(null)
